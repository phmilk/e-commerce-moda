# ---------------------------------------------------------------------------
# Template de ambiente. Copie para .env.local (gitignored).
# ---------------------------------------------------------------------------

# SQLite local. O arquivo dev.db é criado na raiz do projeto no primeiro db:push.
DATABASE_URL=file:./dev.db

# Better Auth
BETTER_AUTH_URL=http://localhost:3000
BETTER_AUTH_SECRET= # pnpm dlx @better-auth/cli secret
